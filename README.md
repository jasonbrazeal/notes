# notes

```bash
# run webpack build
npm run build
# start webpack server
npm run start
npm run --prefix /path/to/project/folder start
# start flask server
NOTES_DIR=/path/to/notes/ FLASK_DEBUG=1 FLASK_APP=/path/to/project/folder/project/app.py flask start
```

